// @generated by Cursor AI (Claude) — verified by Kevin

import { useTranslation } from 'react-i18next';
import { Link } from 'react-router-dom';
import { Shield, Lightbulb, Trash2 } from 'lucide-react';

/**
 * Page d'accueil pour citoyens (Dashboard public)
 * 
 * Contenu :
 * - Héro avec CTA principal
 * - 3 types de signalements rapides (Agression, Eclairage, Déchets)
 */
function HomePage() {
  const { t } = useTranslation('common');

  return (
    <div className="space-y-8">
      {/* Héro Section */}
      <section className="text-center py-6">
        <h2 className="text-3xl md:text-4xl lg:text-5xl text-white font-bold">
          {t('home.hero.title', { defaultValue: 'Signalez les problèmes de votre ville' })}
        </h2>
      </section>

      {/* Types de signalements */}
      <section className="space-y-4">
        {/* Carte Agression */}
        <Link
          to="/signalements/nouveau?type=securite"
          className="group flex items-center gap-4 p-6 bg-white rounded-2xl shadow-md hover:shadow-xl transition-all duration-300 hover:scale-[1.02] active:scale-[0.98] transform"
        >
          <div className="w-16 h-16 rounded-2xl bg-gradient-to-br from-red-500 to-red-600 flex items-center justify-center shadow-lg flex-shrink-0 group-hover:scale-110 transition-transform duration-300">
            <Shield className="w-8 h-8 text-white" />
          </div>
          <div className="flex-1">
            <h3 className="text-2xl md:text-3xl font-bold text-red-600 group-hover:text-red-500 transition-colors">
              {t('home.types.agression', { defaultValue: 'Agression' })}
          </h3>
          </div>
          <div className="text-red-600 opacity-0 group-hover:opacity-100 transition-opacity">
            →
          </div>
        </Link>

        {/* Carte Eclairage */}
        <Link
          to="/signalements/nouveau?type=eclairage"
          className="group flex items-center gap-4 p-6 bg-white rounded-2xl shadow-md hover:shadow-xl transition-all duration-300 hover:scale-[1.02] active:scale-[0.98] transform"
        >
          <div className="w-16 h-16 rounded-2xl bg-gradient-to-br from-amber-500 to-amber-600 flex items-center justify-center shadow-lg flex-shrink-0 group-hover:scale-110 transition-transform duration-300">
            <Lightbulb className="w-8 h-8 text-white" />
          </div>
          <div className="flex-1">
            <h3 className="text-2xl md:text-3xl font-bold text-orange-600 group-hover:text-orange-500 transition-colors">
              {t('home.types.eclairage', { defaultValue: 'Eclairage' })}
          </h3>
          </div>
          <div className="text-orange-600 opacity-0 group-hover:opacity-100 transition-opacity">
            →
          </div>
        </Link>

        {/* Carte Déchets */}
        <Link
          to="/signalements/nouveau?type=dechets"
          className="group flex items-center gap-4 p-6 bg-white rounded-2xl shadow-md hover:shadow-xl transition-all duration-300 hover:scale-[1.02] active:scale-[0.98] transform"
        >
          <div className="w-16 h-16 rounded-2xl bg-gradient-to-br from-green-500 to-green-600 flex items-center justify-center shadow-lg flex-shrink-0 group-hover:scale-110 transition-transform duration-300">
            <Trash2 className="w-8 h-8 text-white" />
          </div>
          <div className="flex-1">
            <h3 className="text-2xl md:text-3xl font-bold text-green-600 group-hover:text-green-500 transition-colors">
              {t('home.types.dechets', { defaultValue: 'Déchets' })}
          </h3>
          </div>
          <div className="text-green-600 opacity-0 group-hover:opacity-100 transition-opacity">
            →
          </div>
        </Link>
      </section>
    </div>
  );
}

export default HomePage;
